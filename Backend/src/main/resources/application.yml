spring:
  application:
    name: tournament-management

  datasource:
    url: jdbc:mysql://localhost:3306/tournament_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: Sahib2611*
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8080

app:
  jwt:
    secret: "ChangeThisToALongRandomSecretKeyForProductionUseOnly123456789"
    expiration-ms: 86400000

firebase:
  # Set to the absolute path of your Firebase service account JSON to enable Firebase auth.
  # Leave blank to run in demo-only mode.
  service-account-path: "C:/Users/bhavp/SJSU/CMPE-131/Class Project/Backend/firebase-service-account.json"

logging:
  level:
    org.springframework.security: INFO
